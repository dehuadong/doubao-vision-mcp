[project]
name = "doubao-vision-mcp"
version = "0.1.1"
description = "MCP server for Doubao Vision model (doubao-seed-2.0-pro)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.1.0",
    "httpx>=0.24.0",
]